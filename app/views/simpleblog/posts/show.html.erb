<div class="container">
	<h2><%= @post.title %></h2>
	<p class="small"><%= @post.date_parsed %></p>

	<%= @post.content.html_safe %>

	<div class="well well-small">
	  <% @post.categories.each do |c| %>
	    <%= link_to c.title, category_path(c) %>
	  <% end %>
	</div>
</div>